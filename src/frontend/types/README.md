# Tipos Compartilhados

Este arquivo re-exporta os tipos de `src/shared/types/index.ts` para facilitar o uso no frontend.

## Uso

```typescript
import type { TutorProfile, AnimalProfile, MatchingRule } from "@/types/shared";
```

Todos os tipos definidos em `src/shared/types/index.ts` estão disponíveis aqui.
