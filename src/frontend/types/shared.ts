/**
 * Re-export de tipos compartilhados
 * Estes tipos são definidos em src/shared/types/index.ts
 * Importar como: import type { TutorProfile, AnimalProfile } from '@/types/shared'
 */

export type {
  Location,
  TutorProfile,
  AnimalProfile,
  MatchingRule,
  MatchResult,
  MatchResponse,
  ComparatorFunction,
} from "@ong-matching-animal/shared/types";
